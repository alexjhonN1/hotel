<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Agregar Nueva Habitación</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f0f2f5;
      padding: 40px;
      display: flex;
      justify-content: center;
    }

    .container {
      background: #fff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
      max-width: 500px;
      width: 100%;
    }

    h1 {
      text-align: center;
      color: #333;
      margin-bottom: 20px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    input, textarea, select {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 15px;
    }

    textarea {
      resize: vertical;
      min-height: 80px;
    }

    button {
      background-color: #4e4376;
      color: white;
      border: none;
      padding: 12px;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #2b5876;
    }

    .volver-btn {
      display: inline-block;
      margin-top: 20px;
      text-align: center;
      background-color: #777;
      color: white;
      padding: 10px 18px;
      border-radius: 6px;
      text-decoration: none;
      transition: background-color 0.3s ease;
    }

    .volver-btn:hover {
      background-color: #555;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>Agregar Nueva Habitación</h1>

    <form action="/habitaciones/nuevo" method="POST" enctype="multipart/form-data">
      <input type="text" name="numero" placeholder="Número de habitación" required>
      <input type="text" name="tipo" placeholder="Tipo (simple, doble, suite)" required>
      <textarea name="descripcion" placeholder="Descripción de la habitación"></textarea>
      <input type="number" name="precio" placeholder="Precio por noche" step="0.01" required>
      <select name="estado" required>
        <option value="disponible">Disponible</option>
        <option value="ocupada">Ocupada</option>
        <option value="mantenimiento">Mantenimiento</option>
      </select>
      <input type="file" name="imagen" accept="image/*">
      <button type="submit">Guardar</button>
    </form>

    <a href="/habitaciones" class="volver-btn">← Volver</a>
  </div>

</body>
</html>
