<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Clientes</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f0f2f5;
      padding: 40px;
    }

    h1 {
      text-align: center;
      color: #333;
      margin-bottom: 30px;
    }

    .btn-agregar {
      display: inline-block;
      margin-bottom: 20px;
      padding: 10px 16px;
      background-color: #4e4376;
      color: white;
      text-decoration: none;
      border-radius: 6px;
      font-weight: bold;
      transition: background-color 0.3s ease;
    }

    .btn-agregar:hover {
      background-color: #2b5876;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background-color: white;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
      overflow: hidden;
    }

    thead {
      background-color: #4e4376;
      color: white;
    }

    th, td {
      padding: 12px 15px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }

    tr:hover {
      background-color: #f1f1f1;
    }

    .acciones a {
      margin-right: 10px;
      color: #4e4376;
      text-decoration: none;
      font-weight: bold;
    }

    .acciones a:hover {
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      table, thead, tbody, th, td, tr {
        display: block;
      }

      tr {
        margin-bottom: 15px;
        border-bottom: 2px solid #eee;
      }

      td {
        text-align: right;
        padding-left: 50%;
        position: relative;
      }

      td::before {
        content: attr(data-label);
        position: absolute;
        left: 15px;
        top: 12px;
        font-weight: bold;
        color: #555;
      }
    }
  </style>
</head>
<body>

  <h1>Clientes</h1>

  <a class="btn-agregar" href="/clientes/nuevo">➕ Agregar Nuevo Cliente</a>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Nombre</th>
        <th>Correo</th>
        <th>Teléfono</th>
        <th>Documento</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% clientes.forEach(cliente => { %>
        <tr>
          <td data-label="ID"><%= cliente.id %></td>
          <td data-label="Nombre"><%= cliente.nombre %></td>
          <td data-label="Correo"><%= cliente.correo %></td>
          <td data-label="Teléfono"><%= cliente.telefono %></td>
          <td data-label="Documento"><%= cliente.documento_identidad %></td>
          <td data-label="Acciones" class="acciones">
            <a href="/clientes/editar/<%= cliente.id %>">✏️ Editar</a>
            <a href="/clientes/eliminar/<%= cliente.id %>" onclick="return confirm('¿Seguro de eliminar?')">🗑️ Eliminar</a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>

</body>
</html>
